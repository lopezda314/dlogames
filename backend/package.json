{
  "name": "dlogames-backend",
  "version": "1.0.0",
  "description": "The primary backend to dlogames. This should be the only backend that the frontend of dlogames.com talks to.",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "start": "nodemon -e js,graphql -x node src/index.js",
    "dev": "nodemon -e js,graphql -x node --inspect src/index.js",
    "deploy": "prisma deploy --env-file variables.env"
  },
  "dependencies": {
    "babel-preset-env": "^1.7.0",
    "dotenv": "6.0.0",
    "graphql": "^0.13.2",
    "graphql-cli": "^2.16.7",
    "graphql-yoga": "1.16.2",
    "nodemon": "^1.18.7",
    "prisma": "1.17.1",
    "prisma-binding": "2.1.6"
  },
  "author": "dlo"
}
